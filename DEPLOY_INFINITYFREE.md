# دليل رفع المشروع على InfinityFree (نسخة Demo)

هذا الدليل يشرح خطوات رفع وتشغيل نظام إدارة المتاجر (Smart Shop) على استضافة InfinityFree المجانية ليكون متاحاً للتجربة (Demo).

## الخطوة 1: تحضير الملفات
1. قم بضغط جميع ملفات المشروع في ملف `ZIP`.
   - تأكد من أن المجلد `src` والملفات الرئيسية (`index.php`, `db.php`, `api.php`, ...) موجودة مباشرة داخل الملف المضغوط وليست داخل مجلد فرعي.

## الخطوة 2: إنشاء قاعدة البيانات
1. سجل الدخول إلى لوحة تحكم InfinityFree (Control Panel).
2. اذهب إلى قسم **Databases** واختر **MySQL Databases**.
3. في خانة **Create New Database**، اكتب اسماً للقاعدة (مثلاً: `shop`) واضغط **Create Database**.
4. بعد الإنشاء، ستظهر لك معلومات قاعدة البيانات في الأسفل (MySQL Details):
   - **MySQL Hostname**: (مثال: `sql123.infinityfree.com`)
   - **MySQL Database Name**: (مثال: `if0_36000000_shop`)
   - **MySQL Username**: (مثال: `if0_36000000`)
   - **MySQL Password**: (كلمة المرور الخاصة بحسابك في الاستضافة - VPanel Password)

## الخطوة 3: رفع الملفات
1. اذهب إلى **Online File Manager** (أو استخدم برنامج FTP مثل FileZilla).
2. افتح المجلد `htdocs`.
3. احذف الملفات الافتراضية الموجودة (مثل `index2.html` إن وجدت).
4. قم برفع ملف `ZIP` الذي جهزته في الخطوة 1.
5. بعد الرفع، اضغط بالزر الأيمن على الملف واختر **Extract** لفك الضغط.

## الخطوة 4: استيراد قاعدة البيانات
1. ارجع للوحة التحكم واختر **phpMyAdmin**.
2. اضغط على زر **Connect Now** بجانب قاعدة البيانات التي أنشأتها.
3. ستفتح واجهة phpMyAdmin. اختر تبويب **Import** من الأعلى.
4. اضغط على **Choose File** واختر ملف `database.sql` الموجود ضمن ملفات المشروع.
5. اضغط على زر **Go** أو **Import** في الأسفل.
6. انتظر حتى تظهر رسالة نجاح الاستيراد (Success).

**ملاحظة:** ملف `database.sql` يحتوي على مستخدم افتراضي:
- **اسم المستخدم:** `admin`
- **كلمة المرور:** `123456`

## الخطوة 5: ربط قاعدة البيانات (`db.php`)
1. ارجع لمدير الملفات (File Manager) داخل مجلد `htdocs`.
2. ابحث عن ملف `db.php` واضغط عليه بالزر الأيمن ثم **Edit**.
3. ستجد إعدادات الاستضافة (InfinityFree) معطلة بعلامات تعليق `/* ... */`.
4. قم بتعديل القيم بما يطابق معلومات قاعدة البيانات التي حصلت عليها في الخطوة 2.

مثال للتعديل:

```php
// InfinityFree / Shared Hosting Settings
$servername = "sql123.infinityfree.com"; // MySQL Hostname
$username = "if0_36000000";              // MySQL Username
$password = "YourVPanelPassword";        // VPanel Password
$dbname = "if0_36000000_shop";           // Database Name
```

5. (اختياري) لتفعيل "وضع التجربة" (Demo Mode) الذي يمنع الحذف والتعديل:
   - غير السطر: `define('DEMO_MODE', false);`
   - إلى: `define('DEMO_MODE', true);`

6. اضغط **Save**.

## مبروك! 🎉
الآن يمكنك الدخول إلى رابط موقعك وتجربة النظام.
سجل الدخول باستخدام:
- **المستخدم:** `admin`
- **كلمة المرور:** `123456`
